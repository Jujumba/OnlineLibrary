CREATE TABLE Person(
id INT GENERATIED BY DEFAULT AS IDENTITY PRIMARY KEY,
name VARCHAR,
);

CREATE TABLE Book(
id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
holder_id INT REFERENCES Person(id),
title VARCHAR,
year INT,
author VARCHAR
);
